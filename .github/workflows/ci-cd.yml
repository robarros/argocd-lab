name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  DOCKER_IMAGE: robarros/argocd-app
  REGISTRY: docker.io

# Permissões necessárias para o GitHub Actions
permissions:
  contents: write
  security-events: write
  actions: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        cd app
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Test application
      run: |
        cd app
        # Testar se a aplicação inicia sem erros
        timeout 10s python app.py || true

    - name: Generate version tags
      id: meta
      run: |
        # Gerar tags baseadas no commit
        COMMIT_SHA=$(git rev-parse --short HEAD)
        TIMESTAMP=$(date +%Y%m%d-%H%M%S)
        
        # Apenas tag com commit SHA
        TAGS="${DOCKER_IMAGE}:${COMMIT_SHA}"
        
        echo "tags=${TAGS}" >> $GITHUB_OUTPUT
        echo "version=${COMMIT_SHA}" >> $GITHUB_OUTPUT
        echo "timestamp=${TIMESTAMP}" >> $GITHUB_OUTPUT

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        build-args: |
          APP_VERSION=${{ steps.meta.outputs.version }}

    - name: Update Kubernetes deployment
      run: |
        # Backup do arquivo original
        cp k8s/hello-world-app/deployment.yaml k8s/hello-world-app/deployment.yaml.bak
        
        # Atualizar a imagem no deployment do Kubernetes
        sed -i "s|image: robarros/argocd-app:.*|image: ${{ env.DOCKER_IMAGE }}:${{ steps.meta.outputs.version }}|g" k8s/hello-world-app/deployment.yaml
        
        # Adicionar/atualizar annotation para forçar rollout de forma mais simples
        # Apenas usar a mudança de imagem é suficiente para trigger do ArgoCD
        # A annotation é opcional, remover para evitar problemas de sintaxe
        echo "Imagem atualizada - ArgoCD detectará automaticamente pela mudança da imagem"
        
        # Mostrar as mudanças feitas
        echo "=== Mudanças no deployment ==="
        diff k8s/hello-world-app/deployment.yaml.bak k8s/hello-world-app/deployment.yaml || true
        rm k8s/hello-world-app/deployment.yaml.bak
        
        # Verificar se a imagem foi atualizada corretamente
        if grep -q "${{ env.DOCKER_IMAGE }}:${{ steps.meta.outputs.version }}" k8s/hello-world-app/deployment.yaml; then
          echo "✅ Imagem atualizada com sucesso para ${{ env.DOCKER_IMAGE }}:${{ steps.meta.outputs.version }}"
        else
          echo "❌ Erro: Imagem não foi atualizada corretamente"
          exit 1
        fi

    - name: Commit and push changes
      if: github.ref == 'refs/heads/main'
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: '🚀 Update deployment image to ${{ env.DOCKER_IMAGE }}:${{ steps.meta.outputs.version }} [skip ci]'
        file_pattern: 'k8s/hello-world-app/deployment.yaml'
        commit_user_name: 'github-actions[bot]'
        commit_user_email: '41898282+github-actions[bot]@users.noreply.github.com'
        commit_author: 'github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>'
        skip_dirty_check: false
        skip_fetch: false

    - name: Create deployment summary
      run: |
        echo "## 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
        echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
        echo "| **Docker Image** | \`${{ env.DOCKER_IMAGE }}:${{ steps.meta.outputs.version }}\` |" >> $GITHUB_STEP_SUMMARY
        echo "| **Commit SHA** | \`${{ steps.meta.outputs.version }}\` |" >> $GITHUB_STEP_SUMMARY
        echo "| **Timestamp** | \`${{ steps.meta.outputs.timestamp }}\` |" >> $GITHUB_STEP_SUMMARY
        echo "| **Branch** | \`${{ github.ref_name }}\` |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📦 Docker Tag Created:" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
        echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🎯 Next Steps:" >> $GITHUB_STEP_SUMMARY
        echo "1. ArgoCD will automatically detect the changes and sync the application" >> $GITHUB_STEP_SUMMARY
        echo "2. Monitor the deployment in your ArgoCD dashboard" >> $GITHUB_STEP_SUMMARY
        echo "3. Verify the application is running with the new image" >> $GITHUB_STEP_SUMMARY

  security-scan:
    runs-on: ubuntu-latest
    needs: build-and-deploy
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: '${{ needs.build-and-deploy.outputs.image-tag }}'
        format: 'sarif'
        output: 'trivy-results.sarif'

    - name: Upload Trivy scan results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v3
      if: always()
      with:
        sarif_file: 'trivy-results.sarif'